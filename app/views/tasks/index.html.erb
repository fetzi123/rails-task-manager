<h1>All restaurants</h1>

<ul>
  <% @tasks.each do |task| %>
    <%= link_to task.title, task_path(task) %>
    <%= link_to "x", task_path(task), method: :delete %>
  <% end %>
</ul>
<%= link_to "Add Task", tasks_new_path(@task) %>
